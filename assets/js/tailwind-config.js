tailwind.config = {
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                rust: '#009494', // Replaced Rust Orange with Teal
                'rust-dark': '#007575',
            },
            fontFamily: {
                sans: ['Outfit', 'sans-serif'],
            }
        }
    }
}
